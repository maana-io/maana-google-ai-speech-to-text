type AudioClip {
  id: ID!
  url: String!
  start: Int!
  end: Int!
}

input AudioClipInput {
  url: String!
  start: Int!
  end: Int!
}

type TextWithTimeStamp {
  id: ID!
  text: String!
  start: Int!
  end: Int!
}

type Query {
  audioToText(audioClip: AudioClipInput!): TextWithTimeStamp!
}
