apiVersion: v1
kind: Service
metadata:
  name: {{ template "audio-analyzer.fullname" . }}
  labels:
    app: {{ template "audio-analyzer.name" . }}
    chart: {{ template "audio-analyzer.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "audio-analyzer.name" . }}
    release: {{ .Release.Name }}
